{
  "name": "root",
  "private": true,
  "workspaces": [
    "userop.js",
    "zerodev-api",
    "packages/*"
  ],
  "scripts": {
    "api:dev": "cd zerodev-api && npm run start",
    "userop:build": "cd userop.js && npm run build"
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
